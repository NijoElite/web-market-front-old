const initialState = {
  cartItems: [],
  showCartDialog: false,
  showMenu: true,
  checkedOutItems: [],
  loggedInUser: null,
};

const rootReducer = (state = initialState, action) => state;

export default rootReducer;
